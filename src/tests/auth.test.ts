describe("Auth tests", () => {
  it("should register test placeholder", () => {
    expect(true).toBe(true);
  });

  it("should login test placeholder", () => {
    expect(true).toBe(true);
  });
});
